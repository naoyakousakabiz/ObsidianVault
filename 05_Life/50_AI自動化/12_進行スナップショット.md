---
date: 2026-04-17
type: note
domain: life
status: active
source: human
generated_at: 2026-04-17
reviewed: true
tags: [ai, automation, snapshot, todo]
---

# 日常AI自動化 進行スナップショット

最終更新: 2026-04-17

---

## 現状（本番稼働中）

### GitHub Actions ワークフロー

| ワークフロー | 役割 | 状態 |
|---|---|---|
| `p1-1-collect.yml` | 毎朝 06:32 → `#life-info` | ✅ 稼働中 |
| `morning-briefing.yml` | 毎朝 06:00 → `#life-briefing` | ✅ 稼働中 |
| `life-reminder.yml` | 各時刻 → `#life-reminder`（5種別） | ✅ 稼働中 |

**夜サマリ（Slack）は廃止済み**。振り返りは Obsidian（Daily / Voice）で手運用。

### ローカル launchd

**全ジョブ停止済み**（クラウド本番に統一）。

```bash
# 確認コマンド（再起動後などに念のため）
launchctl list | grep com.lifeos  # 何も出なければOK
```

---

## Done チェックリスト

- [x] P1-1 / P1-2 / P1-3 の疎通テスト（workflow_dispatch）
- [x] ローカル launchd とクラウドの二重通知を解消
- [x] 朝ブリーフのカレンダー失敗時に警告メッセージを表示
- [x] Slack テンプレートのリデザイン（全3チャンネル）
- [x] GOG_TOKEN_B64 更新手順のドキュメント化

---

## 残TODO

### 中優先（機能追加）

- [ ] **X 連携の有効化** — `X_BEARER_TOKEN` を取得して GitHub Secrets に設定するだけで有効化できる。コスト（API Tier）要確認。

### 低優先（任意の自動化）

- [ ] **`daily-note-create.yml`** — 毎朝、その日の Daily ノート骨子を自動コミット（既存ファイルは上書きしない）。
- [ ] **`eod-push.sh` / launchd** — 終業時に Vault を git commit / push。`install.sh eod-push` で登録可能。
- [ ] **`voice_journal` / `work_end` / `bedtime` のスケジュール追加** — 現在は手動 dispatch のみ。

### バックログ

- [ ] 会話の自動データ化 PoC（音声 → 文字起こし → 要点抽出 → Daily ノートへ保存）

---

## 週次チェック観点

1. GitHub Actions のエラー有無（Actions タブで確認）
2. 重複通知が発生していないか
3. `#life-info` の情報ソースが有効か（リンク切れ・フィード停止）
4. `GOG_TOKEN_B64` の期限（予定欄に警告が出ていないか）
