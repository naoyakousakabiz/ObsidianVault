# [Vault・自律実行] `80_Journal/01_Daily/YYYY-MM-DD.md` を `.cursor/rules/daily-note-voice.mdc` に従い **新規作成** または **既存のみ確認**。**ユーザーに「今日やること」やタスクの箇条書きを聞かない。** 返答は日本語。まず `date +%Y-%m-%d` 等で今日を確定し、**Read/Write でファイル操作**（質問で止めない）。

> 補足: CLI がプロンプトの一部しか渡さない場合に備え、**この1行目の見出しが本文の要約**。以下もすべて実行対象。

## 手順（このコマンド全文がプロンプトのとき）

1. 今日 `YYYY-MM-DD` を確定。
2. `80_Journal/01_Daily/YYYY-MM-DD.md` を読む。
   - **存在する:** 上書き禁止。「既にある」と伝え、ユーザー指定の追記だけ。
   - **無い:** 新規作成。
3. 新規時の YAML: `date:` 今日 / `type: log` / `domain: journal` / `status: active` / `source: human` / `tags: []`
4. 本文は `99_System/Templates/Daily.md` の**展開後**と同一（Templater タグ禁止）。見出し・行数・空行は `.mdc` 厳守。
5. 繰り越し: 直前カレンダー日（無ければ最新1日）の `## 今日やること` ～ 次の `---` を `.mdc` の捨てる行・残す行・上限・見出し正規化で取り込む。推定で文言を埋めない（空 `1.` でよい）。

## 禁止

- `98_NotebookLM_Sync/` 直下の編集。
- クライアント固有情報の記載。

完了報告は新規／既存・繰り越しの有無のみ短文。
